defaults:
  providers:
    tc:
      type: '@faasjs/tencentcloud'
      config:
        appId: appId
        secretId: secretId
        secretKey: secretKey
        region: ap-beijing
  plugins:
    defaults:
      function: func
    func:
      provider: tc
      type: 'function'
    test:
      type: 'defaults'
    http:
      type: 'http'
    mysql:
      type: 'mysql'
local:
  plugins:
    test:
      type: 'local'
