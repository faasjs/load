defaults:
  resources:
    function:
      type: 'function'
    test:
      type: 'defaults'
    http:
      type: 'http'
    mysql:
      type: 'mysql'
local:
  resources:
    test:
      type: 'local'
